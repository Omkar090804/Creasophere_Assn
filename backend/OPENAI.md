## How to integrate OpenAI for real summaries
1. `pip install openai`
2. Set OPENAI_API_KEY as environment variable.
3. Replace the mock summary in `api/views.py` with a call to OpenAI's completion/chat endpoint.
